4 + 4 // an Expression --> produce Value.
2022  // an Expression --> produce Value.
true && false && !false // both an Expression and a statement.


/* a Statements --> 
doesn't produce Value and translate actions that we want to programme to perform
*/

if (18 < 32) {
    const str = '32 is bigger';
}

// in Template literal we must only use Expressions NOT Statements.